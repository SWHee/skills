interface:
  display_name: "Agon"
  short_description: "AI 해커톤의 규칙·문제·증거를 구현 전에 확정하는 실행 스킬"
  default_prompt: "$agon을 사용해 이 AI 해커톤 참가 계획의 준비 상태를 확인하고 다음 단계를 실행해줘."

policy:
  allow_implicit_invocation: true
