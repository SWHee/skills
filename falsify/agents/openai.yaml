interface:
  display_name: "Falsify"
  short_description: "의견과 의문의 전제를 검증하고 핵심 판단과 근거만 전달하는 스킬"
  default_prompt: "$falsify를 사용해 이 의견이나 의문을 검증하고 판단과 핵심 근거만 읽기 쉽게 알려줘."

policy:
  allow_implicit_invocation: false
